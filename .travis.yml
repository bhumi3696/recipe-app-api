 language: python
 python:
    - "3.6"
 services:
    - docker
 install:
    - pip install -r requirements.txt
 before_script:
    - py -m pip install docker-compose
 script:
    - docker-compose run app sh -c "py -m manage.py test"
